# Trackota Environment Variables

# API Base URL
# Leave empty to default to same-origin /api (Vercel serverless). For a remote API, set full URL.
REACT_APP_API_BASE=

# Use Mock Data
# Set to "true" to use hardcoded mock data instead of real datasets (default: false)
REACT_APP_USE_MOCKS=false

# Default dataset folder (relative to data/datasets). Example: "Sebring/Race 1"
# Leave empty to auto-detect first valid folder.
REACT_APP_DATASET_FOLDER=

# Dataset Directory Override (Node API only)
# Override the default data/datasets path (optional)
# TRACKOTA_DATASETS_DIR=/path/to/datasets

